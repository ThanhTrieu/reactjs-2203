import React from 'react';
import RoutesApp from './routes/index';

import './index.css';

const ShoppingApp = () => {
    return (
        <RoutesApp/>
    )
}
export default React.memo(ShoppingApp);